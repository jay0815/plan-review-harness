# 需求：后端枚举值待确认时保留可追踪代码 TODO

短信验证码场景需要传递 `SmsType.RFC`。后端正式枚举值尚未确认，但当前实现可以先用占位值 `21` unblock 编码。

计划必须表达：

- `SmsType.RFC = 21` 是临时占位值。
- 代码中需要标注 TODO，便于 IDE 和静态工具追踪。
- 同一事项必须在待确认项中跟踪，并写清关闭标准。
- 该占位不阻塞当前编码，但正式上线前需要用后端确认值替换。

请生成一份计划，保留代码 TODO 追踪要求，不要把它改写成删除 TODO。
