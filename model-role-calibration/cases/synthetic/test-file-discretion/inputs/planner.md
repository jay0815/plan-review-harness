# 需求：为字符串归一化函数增加空值处理

现有 TypeScript 工程中有一个字符串归一化函数：

- `src/referenced.ts:1-3`
- 符号：`normalizeName`
- 当前行为：接收字符串并返回 `value.trim()`。

本次需求：

- 输入类型扩展为 `string | null | undefined`。
- `null` 和 `undefined` 返回空字符串。
- 非空字符串继续保持 trim 行为。
- 增加覆盖空值、普通字符串和前后空格字符串的测试。
- 不修改其他源文件或项目配置。

当前输入没有提供现有测试文件路径和测试目录结构。测试文件位置、命名、测试 helper 及局部组织方式应由实现者按现有项目惯例决定，不属于需要产品或架构裁决的公共契约。

请生成一份决策完备但不过度预实现的计划。
